# Rifaq Mobile App

This folder is reserved for the Expo React Native customer app.

All future screens must consume brand values through a `useBrandConfig()` hook backed by the shared `BrandConfig` contract from `@rifaq/shared`.
